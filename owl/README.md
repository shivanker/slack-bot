# Camel-Owl v0

Build: `$ docker compose -f owl/docker-compose.yml build`
Run: `$ docker compose -f owl/docker-compose.yml up -d`
Bash: `$ docker compose -f owl/docker-compose.yml exec owl bash`
Then: `$ cd /app && python /app/examples/run_cli.py`
